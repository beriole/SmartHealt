/**
 * Configuration native React Native CLI.
 * `assets` : dossiers liés via `npx react-native-asset` (polices Figtree / Noto Sans).
 */
module.exports = {
  project: {
    ios: {},
    android: {},
  },
  assets: ['./assets/fonts'],
};
